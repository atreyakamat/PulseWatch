# Database connection - local PostgreSQL
# Update user/password/db to match your local instance.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/pulsewatch?sslmode=disable

# Server port
PORT=5000
